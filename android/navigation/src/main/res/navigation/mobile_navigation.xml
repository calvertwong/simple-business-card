<?xml version="1.0" encoding="utf-8"?>
<!--suppress AndroidDomInspection -->

<!-- Fragments are not referenced in this file and the fragment name should be in red.
The reason they are not in red currently is because the errors are being suppressed by line 2.
Although they are not referenced, by providing the correct package and file location will make
this works. -->

<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.calvert.aa.simplebusinesscard.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_gallery"
        android:name="com.calvert.aa.simplebusinesscard.ui.gallery.GalleryFragment"
        android:label="@string/menu_gallery"
        tools:layout="@layout/fragment_gallery" />

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="com.calvert.aa.simplebusinesscard.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_slideshow"
        tools:layout="@layout/fragment_slideshow" />
</navigation>